import React from 'react'

// 1. get id from params (useParams)
// 2. with the id, send api call on load (axios, useEffect)

// 3. form input : useState
// 4. after submit, send request to API: axios
// 5. logic after submit: redirect: useNavigate

// DELETE
// 1. Add the button
// 2. When it is clicked, trigger handleDelete and would send delete request to API using the ID
// 3. After it is deleted, redirect to another page

const EditPage = () => {
  // Create all the state variables for the input

  // useEffect to get information from API, then reset all the state variables

  // handleSubmit to send a put/patch request to the backend

  return (
    <div>
      {/* Same form as create , don't forget to have value in your input. */}


    </div>
  )
}

export default EditPage